{
	"name": "replay-cleanCode",
	"version": "1.0.0",
	"description": "Clean code files for all Replay project.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/motyros/replay-cleanCode.git"
	},
	"author": "motyros",
	"license": "",
	"bugs": {
		"url": "https://github.com/motyros/replay-cleanCode/issues"
	},
	"homepage": "https://github.com/motyros/replay-cleanCode#readme",

	"devDependencies": {
		"eslint": "^2.12.0",
		"eslint-config-google": "^0.6.0",
		"pre-commit": "^1.1.3"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"lint": "eslint $(git diff-index --cached --name-only HEAD | grep .*\\.js$)",
		"lintAll": "eslint ."
	},
	"pre-commit": {
		"run": "lint",
		"colors": true
	}
}
